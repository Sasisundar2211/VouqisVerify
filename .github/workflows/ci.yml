name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    name: vouqis-verify (Python)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/verify
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -e ".[dev]" --quiet
      - run: pytest -q

  dashboard:
    name: dashboard (Node.js)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/vouqis-dashboard
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: packages/vouqis-dashboard/package-lock.json
      - run: npm ci
      # next@16.2.11 is latest stable; sharp/next high CVEs have no non-breaking fix available
      - run: npm audit --audit-level=critical --omit=dev
      - run: npm run build

  test:
    name: test
    runs-on: ubuntu-latest
    needs: [verify, dashboard]
    steps:
      - run: echo "verify and dashboard jobs passed"
